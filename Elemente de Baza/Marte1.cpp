#include <iostream>
using namespace std;
int a, b, c, n, m, p;
int main()
{
	cin >> a >> b >> c >> n >> m >> p;
	cout << a * n + b * m + c * p;
}
// a very straight forward code. we multiply a with n,b with m and c with p and then we sum them up to get the value asked