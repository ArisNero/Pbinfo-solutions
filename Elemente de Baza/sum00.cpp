#include <iostream>
using namespace std;
long a, b,s;
int main()
{
	cin >> a >> b;s = a + b;
	cout << s;
}

// the variables a and b get two numbers. the variable s receives the sum of a+b; long is used so that big numbers do not represent a problem. cout<<s is used to display the sum