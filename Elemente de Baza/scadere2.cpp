#include <iostream>
using namespace std;
long a, b,dif;
int main()
{
	cin >> a >> b;
	dif = a - b;
	cout << dif;
	/*
	* or, instead of using the variable dif, we can just say cout<< a-b; The result is the same
	*/
}